# Preview Assets

Temporary asset copies for `src/features/coachingReportsPreview`.

These Wizards and team-logo assets were copied from the prototype so the preview does not depend on `/public` paths or any deployed copy of the prototype. Delete this folder with the preview feature when testing ends.
